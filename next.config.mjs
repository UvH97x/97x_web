/*
  * next.config.mjs
*/

// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  pageExtensions: ['ts', 'tsx', "md", 'mdx'], // MDXファイルをページとして認識
};

export default nextConfig;
